# AI Head 3D — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el `AzentMark` estático del Hero con una cabeza humana 3D en point cloud que rota automáticamente y reacciona al cursor.

**Architecture:** Nuevo componente `AIHead` que carga el modelo GLTF `LeePerrySmith.glb`, extrae los vértices del mesh, y los renderiza como `<Points>` con colores basados en profundidad (Z). Montado en un `<Canvas>` de React Three Fiber dentro de la tarjeta existente del Hero.

**Tech Stack:** `@react-three/fiber` v8, `@react-three/drei`, `three`, `vitest` + `@testing-library/react` para tests.

---

## Estructura de archivos

| Archivo | Cambio |
|---|---|
| `public/models/LeePerrySmith.glb` | Nuevo asset (descargado) |
| `src/components/ui/AIHead.tsx` | Nuevo componente principal |
| `src/components/ui/AIHead.test.tsx` | Tests del componente y lógica pura |
| `src/components/ui/index.ts` | Exportar `AIHead` |
| `src/routes/index.tsx` | Sustituir `<AzentMark>` por `<AIHead>` |
| `package.json` | Añadir dependencias R3F |

---

### Task 1: Instalar dependencias

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Instalar paquetes R3F**

```bash
cd /Users/andreugarcia/Documents/AZENT/azent-es
pnpm add @react-three/fiber @react-three/drei three
pnpm add -D @types/three
```

Salida esperada: paquetes instalados sin errores, `package.json` actualizado.

- [ ] **Step 2: Verificar instalación**

```bash
node -e "require('./node_modules/three/build/three.cjs')" && echo "OK"
```

Salida esperada: `OK`

- [ ] **Step 3: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "feat: add react-three-fiber and drei dependencies"
```

---

### Task 2: Descargar modelo GLTF

**Files:**
- Create: `public/models/LeePerrySmith.glb`

- [ ] **Step 1: Crear directorio**

```bash
mkdir -p public/models
```

- [ ] **Step 2: Descargar el modelo**

```bash
curl -L \
  "https://raw.githubusercontent.com/mrdoob/three.js/dev/examples/models/gltf/LeePerrySmith/LeePerrySmith.glb" \
  -o public/models/LeePerrySmith.glb
```

Salida esperada: archivo `LeePerrySmith.glb` presente en `public/models/`. Si el curl falla (404), usar esta URL alternativa:
```bash
curl -L \
  "https://threejs.org/examples/models/gltf/LeePerrySmith/LeePerrySmith.glb" \
  -o public/models/LeePerrySmith.glb
```

- [ ] **Step 3: Verificar tamaño del archivo**

```bash
ls -lh public/models/LeePerrySmith.glb
```

Salida esperada: archivo entre 500KB y 5MB (es un modelo 3D de calidad).

- [ ] **Step 4: Commit**

```bash
git add public/models/LeePerrySmith.glb
git commit -m "feat: add LeePerrySmith GLTF head model"
```

---

### Task 3: Escribir tests (TDD — primero en rojo)

**Files:**
- Create: `src/components/ui/AIHead.test.tsx`

- [ ] **Step 1: Crear el archivo de tests**

Crear `src/components/ui/AIHead.test.tsx` con este contenido:

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render } from '@testing-library/react'
import React from 'react'

// ─── Mocks ────────────────────────────────────────────────────────────────────

vi.mock('@react-three/fiber', () => ({
  Canvas: ({ children }: { children: React.ReactNode }) => (
    <div data-testid="r3f-canvas">{children}</div>
  ),
  useFrame: vi.fn(),
}))

vi.mock('@react-three/drei', () => ({
  useGLTF: vi.fn(() => ({
    scene: {
      traverse: vi.fn(),
    },
  })),
}))

// ─── Tests de la función pura depthToColor ────────────────────────────────────

import { depthToColor } from '#/components/ui/AIHead'

describe('depthToColor', () => {
  it('devuelve color frontal (#E6E6E6 ≈ 0.902) cuando z = maxZ', () => {
    const [r, g, b] = depthToColor(10, 0, 10)
    expect(r).toBeCloseTo(0.902, 1)
    expect(g).toBeCloseTo(0.902, 1)
    expect(b).toBeCloseTo(0.902, 1)
  })

  it('devuelve color trasero (#2E2E2E ≈ 0.18) cuando z = minZ', () => {
    const [r, g, b] = depthToColor(0, 0, 10)
    expect(r).toBeCloseTo(0.18, 1)
    expect(g).toBeCloseTo(0.18, 1)
    expect(b).toBeCloseTo(0.18, 1)
  })

  it('devuelve punto medio cuando z está a mitad del rango', () => {
    const [r] = depthToColor(5, 0, 10)
    const expected = (0.902 + 0.18) / 2
    expect(r).toBeCloseTo(expected, 1)
  })

  it('maneja rango degenerado (minZ === maxZ) sin NaN', () => {
    const [r, g, b] = depthToColor(5, 5, 5)
    expect(Number.isNaN(r)).toBe(false)
    expect(Number.isNaN(g)).toBe(false)
    expect(Number.isNaN(b)).toBe(false)
  })
})

// ─── Smoke test del componente ────────────────────────────────────────────────

import { AIHead } from '#/components/ui/AIHead'

describe('AIHead', () => {
  it('renderiza el canvas sin lanzar errores', () => {
    const { getByTestId } = render(<AIHead />)
    expect(getByTestId('r3f-canvas')).toBeDefined()
  })
})
```

- [ ] **Step 2: Ejecutar tests — deben fallar**

```bash
pnpm vitest run src/components/ui/AIHead.test.tsx
```

Salida esperada: FAIL — `Cannot find module '#/components/ui/AIHead'`

---

### Task 4: Implementar `AIHead.tsx`

**Files:**
- Create: `src/components/ui/AIHead.tsx`

- [ ] **Step 1: Crear el componente**

Crear `src/components/ui/AIHead.tsx`:

```tsx
import { useRef, useMemo, Suspense } from 'react'
import { Canvas, useFrame } from '@react-three/fiber'
import { useGLTF } from '@react-three/drei'
import * as THREE from 'three'

// ─── Color de profundidad: blanco frontal → gris trasero ──────────────────────

export function depthToColor(
  z: number,
  minZ: number,
  maxZ: number,
): [number, number, number] {
  const range = maxZ - minZ
  const t = range === 0 ? 0.5 : (z - minZ) / range
  // t=1 → frontal (#E6E6E6 = 0.902), t=0 → trasero (#2E2E2E = 0.18)
  const front = 0.902
  const back = 0.18
  const v = back + (front - back) * t
  return [v, v, v]
}

// ─── Point cloud de la cabeza ─────────────────────────────────────────────────

function HeadPointCloud() {
  const { scene } = useGLTF('/models/LeePerrySmith.glb')
  const groupRef = useRef<THREE.Group>(null)
  const autoRotY = useRef(0)
  const mouseSmooth = useRef({ x: 0, y: 0 })

  const geometry = useMemo(() => {
    const allPositions: number[] = []
    const allColors: number[] = []
    let minZ = Infinity
    let maxZ = -Infinity

    // Primer paso: calcular rango Z global
    scene.traverse((child) => {
      if (child instanceof THREE.Mesh) {
        const pos = child.geometry.attributes.position as THREE.BufferAttribute
        child.updateWorldMatrix(true, false)
        const mat = child.matrixWorld
        const v = new THREE.Vector3()
        for (let i = 0; i < pos.count; i++) {
          v.set(pos.getX(i), pos.getY(i), pos.getZ(i)).applyMatrix4(mat)
          if (v.z < minZ) minZ = v.z
          if (v.z > maxZ) maxZ = v.z
        }
      }
    })

    // Segundo paso: extraer posiciones y colores
    scene.traverse((child) => {
      if (child instanceof THREE.Mesh) {
        const pos = child.geometry.attributes.position as THREE.BufferAttribute
        const mat = child.matrixWorld
        const v = new THREE.Vector3()
        for (let i = 0; i < pos.count; i++) {
          v.set(pos.getX(i), pos.getY(i), pos.getZ(i)).applyMatrix4(mat)
          allPositions.push(v.x, v.y, v.z)
          const [r, g, b] = depthToColor(v.z, minZ, maxZ)
          allColors.push(r, g, b)
        }
      }
    })

    const geo = new THREE.BufferGeometry()
    geo.setAttribute('position', new THREE.Float32BufferAttribute(allPositions, 3))
    geo.setAttribute('color', new THREE.Float32BufferAttribute(allColors, 3))
    return geo
  }, [scene])

  useFrame((state, delta) => {
    if (!groupRef.current) return

    // Rotación automática
    autoRotY.current += delta * 0.3

    // Seguimiento de ratón (interpolado)
    const targetX = -state.pointer.y * (Math.PI / 12)
    const targetY = state.pointer.x * (Math.PI / 12)
    mouseSmooth.current.x += (targetX - mouseSmooth.current.x) * 0.05
    mouseSmooth.current.y += (targetY - mouseSmooth.current.y) * 0.05

    groupRef.current.rotation.x = mouseSmooth.current.x
    groupRef.current.rotation.y = autoRotY.current + mouseSmooth.current.y
  })

  return (
    <group ref={groupRef}>
      <points geometry={geometry}>
        <pointsMaterial
          size={0.012}
          vertexColors
          sizeAttenuation
          transparent
          opacity={0.9}
        />
      </points>
    </group>
  )
}

useGLTF.preload('/models/LeePerrySmith.glb')

// ─── Componente público ───────────────────────────────────────────────────────

export function AIHead() {
  return (
    <Canvas
      camera={{ position: [0, 0, 4], fov: 45 }}
      dpr={[1, 1.5]}
      frameloop="always"
      gl={{ alpha: true }}
      style={{ background: 'transparent', width: '100%', height: '100%' }}
    >
      <Suspense fallback={null}>
        <HeadPointCloud />
      </Suspense>
    </Canvas>
  )
}
```

- [ ] **Step 2: Ejecutar tests — deben pasar**

```bash
pnpm vitest run src/components/ui/AIHead.test.tsx
```

Salida esperada: todas las suites en PASS.

- [ ] **Step 3: Ejecutar todos los tests para comprobar que no hay regresiones**

```bash
pnpm test
```

Salida esperada: PASS sin cambios en los tests previos.

- [ ] **Step 4: Commit**

```bash
git add src/components/ui/AIHead.tsx src/components/ui/AIHead.test.tsx
git commit -m "feat: add AIHead point cloud component with depth-based coloring"
```

---

### Task 5: Exportar e integrar

**Files:**
- Modify: `src/components/ui/index.ts`
- Modify: `src/routes/index.tsx`

- [ ] **Step 1: Añadir export en `src/components/ui/index.ts`**

Leer el archivo actual y añadir la línea de export de `AIHead`. El archivo debería quedar con todas las exportaciones existentes más:

```ts
export { AIHead } from './AIHead'
```

- [ ] **Step 2: Actualizar `src/routes/index.tsx`**

En `src/routes/index.tsx`:

Cambiar la línea de import (línea 2):
```ts
// ANTES
import { AzentMark } from '#/components/brand'
// DESPUÉS — eliminar esta línea completamente (AzentMark ya no se usa)
```

Añadir `AIHead` al import de ui (línea 3):
```ts
// ANTES
import { Eyebrow, Button } from '#/components/ui'
// DESPUÉS
import { Eyebrow, Button, AIHead } from '#/components/ui'
```

Reemplazar el bloque del `AzentMark` dentro de `LandingHero` (las líneas 184–210, el div con la marca):

```tsx
{/* ANTES — eliminar todo este bloque: */}
{/* Brand mark */}
<div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
  <div
    style={{
      position: 'relative',
      width: 340,
      height: 340,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: 'var(--bg-surface)',
      border: '1px solid var(--border-subtle)',
      borderRadius: 'var(--radius-2xl)',
    }}
  >
    <div
      style={{
        position: 'absolute',
        inset: 0,
        background: 'radial-gradient(circle at 50% 50%, rgba(255,90,31,0.16), transparent 58%)',
        borderRadius: 'var(--radius-2xl)',
        animation: 'az-glow-breathe 5s ease-in-out infinite',
      }}
    />
    <AzentMark size={210} color="#FF5A1F" />
  </div>
</div>

{/* DESPUÉS — sustituir por: */}
{/* AI Head */}
<div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
  <div
    style={{
      position: 'relative',
      width: 340,
      height: 340,
      background: 'var(--bg-surface)',
      border: '1px solid var(--border-subtle)',
      borderRadius: 'var(--radius-2xl)',
      overflow: 'hidden',
    }}
  >
    <AIHead />
  </div>
</div>
```

- [ ] **Step 3: Verificar TypeScript**

```bash
pnpm exec tsc --noEmit
```

Salida esperada: 0 errores.

- [ ] **Step 4: Commit**

```bash
git add src/components/ui/index.ts src/routes/index.tsx
git commit -m "feat: replace AzentMark with AIHead 3D point cloud in hero"
```

---

### Task 6: Ajuste visual y verificación

**Files:**
- Modify: `src/components/ui/AIHead.tsx` (si hay que ajustar escala/cámara)

El modelo LeePerrySmith puede necesitar ajuste de escala o posición de cámara dependiendo de sus dimensiones reales. Esta tarea cubre ese ajuste.

- [ ] **Step 1: Iniciar dev server**

```bash
pnpm dev
```

Abrir `http://localhost:3000` en el navegador.

- [ ] **Step 2: Verificar que la cabeza aparece y rota**

Comprobar que:
- La nube de puntos de la cabeza humana es visible
- Rota continuamente en el eje Y
- Al mover el ratón encima, la cabeza sigue el cursor suavemente
- Los puntos frontales son más claros (blancos) y los traseros más oscuros (grises)

- [ ] **Step 3 (si hace falta): Ajustar escala y cámara**

Si la cabeza se ve demasiado grande, pequeña, o mal encuadrada, editar en `AIHead.tsx`:

Para ajustar escala del modelo:
```tsx
// Dentro de HeadPointCloud, cambiar el <group> por:
<group ref={groupRef} scale={[0.08, 0.08, 0.08]}>
```
Probar valores entre `0.05` y `0.15` hasta que llene ~85% del canvas.

Para ajustar la cámara (en `<Canvas>`):
```tsx
// Alejar cámara si el modelo es grande:
camera={{ position: [0, 0, 6], fov: 45 }}
// Acercar si es pequeño:
camera={{ position: [0, 0, 2], fov: 45 }}
```

Para centrar verticalmente (si la cabeza está desplazada):
```tsx
// Dentro de HeadPointCloud, añadir position al group:
<group ref={groupRef} position={[0, -0.5, 0]}>
```

- [ ] **Step 4: Commit final**

```bash
git add src/components/ui/AIHead.tsx
git commit -m "fix: adjust AIHead camera and scale for hero layout"
```

---

## Notas de implementación

- El `frameloop="always"` es necesario porque hay animación continua (rotación constante).
- `state.pointer` en R3F devuelve coordenadas normalizadas (-1 a 1); solo se actualiza con eventos de ratón, así que en móvil (sin ratón) el mouse tracking sencillamente no tiene efecto.
- El `depthToColor` se exporta como named export para facilitar los tests.
- Si el modelo tiene varios meshes (pelo, dientes, etc.), el traverse captura todos y los combina en una sola geometría de puntos.
